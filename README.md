# 3dSpaceship

**Give the bots a try** — make a new directory, feed Claude this repo's URL, and say _"clone this repo, install the dependencies, and start the server so I can play."_

Doing it by hand: `npm install && npm run dev`, then open http://localhost:2567.

`npm run help` prints the URLs other players can connect on.
